# 发现记录

- 当前目录初始只有 `projects/` 和 `outputs/`。
- 当前目录初始不是 Git 仓库。
- `D:\Application\Anaconda\envs\sports3d` 已安装 `pose2sim 0.10.43`。
- 同环境可导入 `opensim 4.5.1`、`customtkinter 5.2.2`、`PySide6`、`plotly 6.8.0`、`openpyxl 3.1.5`。
- 系统可找到 `ffmpeg`。
- 本机没有 `gh` 命令；发布只能先使用普通 Git，认证失败时需要用户处理凭据。
- 官方 Pose2Sim 输出：`.trc` 在 `pose-3d`，`.mot` 和 `.osim` 在 `kinematics`，日志为 `logs.txt`。

