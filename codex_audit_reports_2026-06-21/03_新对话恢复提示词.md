# 新对话恢复提示词

把下面内容复制到新的 Codex 对话中，可以快速恢复本次上下文。

```text

工作区：
D:\Application\Biomechanics\Pose2sim_Pipeline

项目定位：
这是 perfanalytics/pose2sim 的全中文 .BAT 启动图形化界面。目标用户是非编程、非生物力学背景的体能教练/康复师，希望他们尽量在一个中文 GUI 中完成 Pose2Sim 的采集导入、校准、运行和报告输出。

重要约束：
- 不要修改pose2sim的源码。
- 用户输入和输出在 projects/、outputs/，默认应避免提交。
- 每次代码改动后都要提交并推送到 GitHub，提交说明要写清楚修改原因、兼容目的和验证结果，方便以后上游修复后回滚 GUI 侧临时兼容代码。

当前架构摘要：
- 启动：launch_pose2sim_gui.bat -> D:\Application\Anaconda\envs\sports3d\python.exe -m pose2sim_pipeline_gui
- GUI 主体：pose2sim_pipeline_gui/app.py，CustomTkinter，六个页签：环境、项目、校准、视频、参数、运行。
- 项目/导入：workspace.py 创建 projects/<项目> 和 outputs/<项目>，导入视频/校准资料，ffmpeg 转码。
- 配置：config_builder.py 生成 Pose2Sim Config.toml，config_adapter.py 从已有 Config 回填和合并。
- 运行：runner.py 启动子进程，pipeline_cli.py 调用 Pose2Sim.Pose2Sim.Pose2SimPipeline 的 calibration、poseEstimation、synchronization、personAssociation、triangulation、filtering、markerAugmentation、kinematics，并附加 reports。
- 报告：report.py 读取 OpenSim .mot，结合 joint_metadata.py 生成中文 HTML/Excel；results_export.py 镜像 Pose2Sim 输出到 outputs/<项目>。

已验证：
- 目标环境为 D:\Application\Anaconda\envs\sports3d\python.exe。
- sports3d 已统一重建到 Python 3.11.15；Pose2Sim 0.10.47；Sports2D 0.8.32；OpenSim 4.5.2；NumPy 1.26.4；pandas 2.3.3；scipy 1.15.3。
- pip check 已通过；Pose2Sim GUI 与 Sports2D GUI 均完成基本导入/启动/测试验证。
- D:\Application\Anaconda\envs\sports3d\python.exe -m pytest 通过，当前为 63 passed。
- compileall pose2sim_pipeline_gui tests 通过。
- 上游 GitHub releases 在 2026-06-21 已显示 0.10.43 后续版本记录，至少有 v0.10.44、v0.10.45，页面还出现 v0.10.45...v0.10.47 changelog 链接；上游 v0.10.44 记录包含安装方式/配置语义变化。

本轮已完成的重要改动：
- 不改 Pose2Sim 源码，只在 GUI 层兼容 Pose2Sim 0.10.47。
- environment.py 将 GUI 验证版本升到 0.10.47，增加 Python 3.10 无法安装 Pose2Sim 0.10.44+ 的中文提示。
- models.py / config_builder.py / config_adapter.py 适配 Pose2Sim 0.10.44+ 配置语义：large_hip_knee_angles 默认 90；trimmed_extrema_percent 默认 50；读取旧 Config 中 0.5 时按旧比例迁移为 50；生成 kinematics.filter_ik=false 与 kinematics.ik_filter_type="acc_minimizing"，并保留旧 filtering.filter_ik=false。
- app.py 增加 Lower_body 姿态模型选项，仅在 Pose2Sim >= 0.10.44 时允许使用；否则中文警告。
- pipeline_cli.py 在运行时继续做旧 trimmed_extrema_percent 比例到百分比的兼容转换，避免旧 Config 在 0.10.47 下静默改变含义。
- 发现 Pose2Sim 0.10.47 的 calibration.extract_frames() 有上游 bug：源码中有 Path(video_path).exists().stem，导致内参视频抽帧异常被上游 broad except 吞掉，随后 cv2.imread(.mp4) 返回 None 并在 img.shape 崩溃。
- 新增 GUI 侧临时兼容层：environment.py 检测安装的 Pose2Sim 是否存在上述源码特征；video.py 用 OpenCV VideoCapture / cv2.imwrite 按官方帧序号 0, fps, 2*fps... 生成 camXX_intrinsics_00000.png；workspace.py 在内参视频导入后、运行/保存前自动准备 PNG；calibration_extension("intrinsics") 优先选择 PNG/JPG；pipeline_cli.py 对旧 Config 中 intrinsics_extension="mp4" 但目录已有 PNG 的项目，在内存中临时切换为 png。
- runner.py 给子进程设置 PYTHONIOENCODING=utf-8，修复 GUI 日志中文乱码。
- 已给 projects\20260621test3 做本地修复：cam01 生成 27 张内参 PNG，cam02 生成 26 张内参 PNG；Config.toml 的 intrinsics_extension 已由 mp4 改为 png，并备份为 Config.backup_intrinsics_png_20260621.toml。projects/ 默认不提交。
- 新增 Caliscope 无感接入：pose2sim_pipeline_gui/caliscope_bridge.py 负责创建 projects/<项目>/caliscope_workspace、发现 Caliscope 导出的兼容校准结果、校验相机数量与 Pose2Sim 必需字段、归档旧 Calib_caliscope.toml、复制为 calibration/Calib_caliscope.toml，并触发 Config 自动切换为 calibration_type="convert" / convert_from="caliscope"。
- 新增 pose2sim_pipeline_gui/caliscope_launcher.py：优先通过 Caliscope 内部 MainWindow.launch_workspace 直接打开当前项目专用工作区；若 Caliscope 内部接口变化，则把该工作区写入 Caliscope 最近项目并回退到官方 CLI 启动。仍不修改 Caliscope 源码。
- app.py 校准页在选择 Caliscope 时隐藏通用“导入外部校准文件”，改为中文向导按钮：“准备并打开 Caliscope 校准”、“我已完成，导入校准结果”、“打开 Caliscope 工作区”。用户不需要识别 camera_array_aniposelib.toml 等内部文件名。
- environment.py / models.py 增加 Caliscope 与 PySide6 检查，并在环境页提供“安装/修复 Caliscope GUI”按钮，使用当前 sports3d Python 执行 pip install --upgrade --upgrade-strategy only-if-needed caliscope[gui]。
- workspace.py 增加 analysis_inputs_newer_than_outputs()；app.py 运行前会检测 Config 或 calibration 顶层 TOML 是否新于已有 .mot，避免导入新 Caliscope 校准后误判成“仅生成报告”。
- app.py 在 Caliscope 操作按钮旁新增“i”信息按钮，改用可滚动中文说明窗口；说明从概念说明改为可照做的步骤：先在中文 GUI 导入内参/外参视频，点击“准备并打开 Caliscope 校准”，再在 Caliscope 依次完成 Project、Cameras/Calibrate、Multi-Camera/Start Processing、Capture Volume/Calibrate，最后回中文 GUI 点击“我已完成，导入校准结果”。测试确保说明不向普通用户暴露 camera_array/aniposelib/toml 等内部文件名。

以后上游修复 Pose2Sim 抽帧 bug 后的恢复要点：
- 先检查当前安装的 Pose2Sim.calibration.extract_frames 源码是否仍包含 Path(video_path).exists().stem。
- 如果上游修复且官方抽帧输出目录也正确，应移除或停用 GUI 侧预抽帧路径：pose2sim_intrinsics_video_extraction_bug_present、extract_calibration_video_frames、workspace.prepare_intrinsics_image_frames 相关调用，以及旧 Config mp4->png 的运行时切换测试。
- 如果上游只修复 .exists().stem，但仍把 PNG 写到当前工作目录而不是 calibration/intrinsics/camXX，需要保留 GUI 侧兼容或继续评估。
- 移除临时兼容前必须用实际项目验证：内参视频导入、Config 生成、calibration 阶段、完整 pipeline；确认不再出现 cv2.imread(.mp4) 的 NoneType.shape 崩溃。

Caliscope 接入维护要点：
- Caliscope 官方输出 camera_array_aniposelib.toml；GUI 内部会自动导入该文件，但界面文案刻意不暴露此文件名给普通用户。
- Pose2Sim 0.10.47 对 convert_from in {"anipose","freemocap","caliscope"} 会跳过转换，后续 triangulation 读取 calibration 目录最新 .toml；因此 GUI 导入时必须确保 calibration/Calib_caliscope.toml 是顶层最新校准 TOML，并把旧同名文件归档到 calibration/_archive/。
- Caliscope 标签不可点的根因是工作区没有按它的约定放入校准视频。GUI 现在会在启动前把 Pose2Sim 项目中的 calibration/intrinsics/camXX/camXX_intrinsics.mp4 映射为 caliscope_workspace/calibration/intrinsic/cam_N.mp4，并把 calibration/extrinsics/camXX.mp4 映射为 caliscope_workspace/calibration/extrinsic/cam_N.mp4；优先硬链接，失败时复制。至少需要 2 个测试视频、2 个校准机位，且测试视频/内参视频/外参视频数量必须一致；外参只有图片或场景点时会阻止启动并提示用户重新导入外参视频。准备流程会先完整校验，校验通过后才清理旧 cam_*.mp4 并重建映射，避免残留旧机位或校验失败时破坏已有 Caliscope 工作区。
- Caliscope 目前没有稳定命令行参数可直接传工作区路径；GUI 通过本地 launcher 桥接其 Python API。若未来 Caliscope 官方支持 CLI workspace 参数，应优先切换到官方接口，减少对内部 API 的依赖。
- 如果未来 Caliscope 改变工作区目录或文件命名约定，优先更新 pose2sim_pipeline_gui/caliscope_bridge.py 的 prepare_caliscope_workspace()，不要在 GUI 中暴露内部目录名；如果 Caliscope 提供官方“从指定视频创建项目/导出 Pose2Sim 校准”的稳定接口，应替换当前硬链接/复制桥接层。
- v1 只做“启动外部 Caliscope + 自动接回 Pose2Sim”，不在中文 GUI 内重写 Caliscope 的完整校准流程。

当前已知注意点：
- 20260621test3 的内参阶段 smoke 已越过原始 NoneType.shape 崩溃；后续失败发生在 extrinsics_method="scene" 的外参场景点手动点击路径。该项目补充信息中 cam02 漏拍了一个场景点，这会影响外参质量/可完成性，属于外参采集与交互问题，不是内参抽帧 bug。
- OpenSim 这次固定在 4.5.2，暂不升 4.6；如需升级 OpenSim，应单独评估 Python/NumPy/OpenSim 组合与 Pose2Sim/Sports2D 的影响。
- 本机没有 gh CLI，不能自动创建 GitHub draft PR；但可以用普通 git commit / git push 推送到 origin。

```
